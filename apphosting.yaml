build: npm run build

# publicDir: backend/public  <-- TENTO RIADOK ZMAŽTE

backend:
  id: "backend-firebase"
  root: backend
  run: npm run start

rewrites:
  # Všetky cesty presmeruj na backend,
  # ktorý následne obslúži SPA (React)
  - source: "/**"
    backend:
      id: "backend-firebase"
